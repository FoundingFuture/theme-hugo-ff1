baseURL = "https://example.org/"
title = "FoundingFuture I"
locale = "en"
theme = "foundingfuture-i"

# Needed only for a shortcode nested inside columns, as on the front page:
# the nested one has become HTML by the time columns renders its Markdown,
# and Goldmark drops that HTML unless this is on. Content never needs raw
# HTML otherwise, and a site without nested shortcodes can leave this out.
[markup.goldmark.renderer]
  unsafe = true

[params]
  tagline = "A Hugo theme for topics of any depth, and a lot of text on one screen"
  established = "a hugo theme"
  domains = ["example.org"]
  # Topics take these in order unless a section sets colour itself.
  palette = ["teal", "indigo", "rose", "amber"]

# Anything that is not a topic. The rail lists these after the tree.
[[params.extraNav]]
  name = "Tags"
  url = "/tags/"

[[params.extraNav]]
  name = "Search"
  url = "/search/"

[[params.extraNav]]
  name = "Screenshots"
  url = "/screenshots/"
